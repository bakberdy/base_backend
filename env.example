APP_ENV selects which file pydantic-settings loads: development.env or production.env.
Set APP_ENV before starting the process (see Makefile). Do not set APP_ENV inside those files.

Local: copy or edit development.env and production.env in the project root.
Docker: compose sets DATABASE_URL for the api container; other keys come from the env file.
